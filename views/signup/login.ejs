<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Login - MentorMap</title>

    <style>
        /* Base Styles */
        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #f4f7fa, #fbe4ec);
            margin: 0;
            padding: 0;
        }

        /* Heading */
        h2 {
            margin-top: 2rem;
            text-align: center;
            color: #444;
            font-size: 2rem;
        }

        /* Container */
        .login-container {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            min-height: 60vh;
        }

        /* Form Styles */
        form {
            background-color: rgb(255, 245, 250);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 3rem 2.5rem;
            border-radius: 15px;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 420px;
            min-width: 300px; /* Ensures better size on small devices */
            border: 1px solid #ddd;
            min-height: 420px;
        }

        /* Labels */
        label {
            display: block;
            margin-bottom: 5px;
            font-size: 1rem;
            color: #333;
        }

        /* Inputs */
        input {
            padding: 0.8rem 1.3rem;
            border-radius: 25px;
            border: 1px solid #ccc;
            font-size: 1rem;
            width: 90%;
            outline: none;
            transition: border 0.3s ease;
        }

        input:focus {
            border-color: #d081a8;
        }

        /* Form Group */
        .form-group {
            width: 100%;
            margin-bottom: 1.2rem;
        }

        /* Button */
        .btn-login {
            background: linear-gradient(135deg, #28a745, #218838);
            color: white;
            margin-top: 15px;
            border-radius: 25px;
            padding: 0.7rem;
            font-size: 1rem;
            border: none;
            cursor: pointer;
            width: 90%;
            transition: background 0.3s ease;
        }

        .btn-login:hover {
            background: linear-gradient(135deg, #218838, #1e7e34);
        }

        /* Links */
        .links {
            margin-top: 1.5rem;
            font-size: 0.95rem;
            text-align: center;
        }

        .links a {
            color: #d081a8;
            text-decoration: none;
            font-weight: 500;
        }

        .links a:hover {
            text-decoration: underline;
        }

        /* Flash Messages */
        .flash-overlay {
            position: fixed;
            top: 80px;
            left: 50%;
            transform: translateX(-50%);
            padding: 10px 20px;
            border-radius: 6px;
            font-size: 1rem;
            z-index: 9999;
            opacity: 1;
            transition: opacity 0.6s ease;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
        }

        .success-flash {
            background-color: #25D366;
            color: #155724;
        }

        .error-flash {
            background-color: #f7a7ae;
            color: #721c24;
        }

        .flash-overlay.hide {
            opacity: 0;
            pointer-events: none;
        }

        /* Tablets */
        @media (max-width: 768px) {
            h2 {
                font-size: 1.8rem;
                margin-top: 1.5rem;
            }

            form {
                padding: 2.5rem 2rem;
                max-width: 95%;
                min-height: 400px;
            }
        }

        /* Mobile Phones */
        @media (max-width: 480px) {
            h2 {
                font-size: 1.5rem;
                margin-top: 1rem;
            }

            form {
                padding: 2rem 1.5rem;
                border-radius: 12px;
                max-width: 90%;
                min-width: unset;
                min-height: 380px;
            }

            input {
                /* padding: 0.7rem 1rem; */
                font-size: 0.95rem;
            }

            .btn-login {
                padding: 0.65rem;
                font-size: 0.95rem;
            }
        }
    </style>
</head>
<body>
    <%- include("../include/flash.ejs") %>
    <h2>Login to MentorMap</h2>
    <div class="login-container">
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" name="username" id="username" placeholder="Enter your username" required>
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" name="password" id="password" placeholder="Enter your password" required>
            </div>

            <button type="submit" class="btn-login">Login</button>

            <div class="links">
                <p>Don't have an account? <a href="/signup">Sign up</a></p>
            </div>
        </form>
    </div>

    <script>
    window.addEventListener('DOMContentLoaded', () => {
        const flash = document.querySelector('.flash-overlay');
        if (flash) {
            setTimeout(() => {
                flash.classList.add('hide');
            }, 2000);
        }
    });
    </script>
</body>
</html>
